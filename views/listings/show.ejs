<% layout("/layouts/boilerplate") %>
<body>
    
    <!-- mt==margin from top 3 diya -->
    <div class="row mt-3" >

        <div class="col-8 offset-2">
            <h3 style="color:crimson;"><%=listing.title%></h3>
            <br>
        </div>
        <!-- ek bootstrap ke card me pura data show kiya -->
        <div class="card col-6 offset-2
        listing-card" >
            <img class="show-img"src="<%=listing.image.url%>" alt="image">
            <div class="card-body bg-body-light">
                <p class="card-text">
                    
                    <i>Owned By: <%=listing.owner.username%></i>
                    <br><br>
                    <b><%= listing.title %></b>
                    <br><br>
    
                    <%= listing.description %>
                    <br><br>
                    &#8377;<%=listing.price.toLocaleString("en-IN")%>
                    <br>
                    <%=listing.location %>
                    <br>
                    <%=listing.country%>
                    <br><br>

                    <!-- btn unhi user ko dikhe jo unke owner -->
                    <!-- agar midware me error to currUser store n hua islliye if m -->

                    <!-- agar curruser logged ho aur -->
                    <% if(currUser && listing.owner._id.equals(currUser._id)) { %>
                        <div class="btns">
                            <a href="/listings/<%=listing._id%>/edit" class="btn btn-dark add-btn">Edit</a>
                            &nbsp;&nbsp;&nbsp;&nbsp;
                            <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE">
                                <button class="btn btn-dark">Delete</button>
                            </form>
                
                        </div>
            
                    <br>
                    <% } %>
                   
                <hr>
                <% if(currUser) { %>
                    <div class="col-8 ">
                        <h4>Leave a Review</h4>
                        <!-- validations=== -->
                        <!-- step1=novalidate -->
                        <!-- step2=class=need-validate -->
                        <!-- step3=textarea=required -->

                        <!-- for server(hopscotch) validations -->
                        <!-- step1=joi schema -->
                        <!-- step2=schema validate func -->
                        <!-- middleware  -->
                        <form method="POST" action="/listings/<%=listing._id%>/reviews"
                            novalidate 
                            class="needs-validation">
                            <!-- mb==marginbottom3 mt==margintop-->
                            <div class="mb-3 mt-3">
                                
                
                                <!-- github se copy paste -->
                                <label for="rating" class="form-label">Rating</label>
                                <fieldset class="starability-growRotate">
                                    <!--  -->
                                    <!-- <legend>First rating:</legend> -->
                                    <!-- <input type="radio" id="no-rate" class="input-no-rate" name="review[rating]" value="0" checked aria-label="No rating." /> -->
                                    <input type="radio" id="first-rate1" name="review[rating]" value="1" />
                                    <label for="first-rate1" title="Terrible">1 star</label>
                                    <input type="radio" id="first-rate2" name="review[rating]" value="2" />
                                    <label for="first-rate2" title="Not good">2 stars</label>
                                    <input type="radio" id="first-rate3" name="review[rating]" value="3" />
                                    <label for="first-rate3" title="Average">3 stars</label>
                                    <input type="radio" id="first-rate4" name="review[rating]" value="4" />
                                    <label for="first-rate4" title="Very good">4 stars</label>
                                    <input type="radio" id="first-rate5" name="review[rating]" value="5" />
                                    <label for="first-rate5" title="Amazing">5 stars</label>
                                </fieldset>


                            </div>
                            <div class="mb-3 mt-3">
                                <label for="comment" class="form-label">Comment Here</label>
                                <textarea
                                name="review[comment]"
                                id="comment"
                                cols="50"
                                rows="5"
                                class="form-control"
                                required
                                ></textarea>

                                <div class="invalid-feedback">Please add some comments for review</div>

                            </div>
                            
                            <!-- review post hoga review db me -->
                            <!-- chuki review connected h listingid se isliye -->
                            <!-- send req to /listings/:id/reviews -->
                            <button class="btn btn-outline-dark">Submit</button>
                        </form>

                    
                    </div>
                    <hr>

                <% } %>
                    
                    <!-- isse objid print to listing pass krte time populate kr do-->
                    
                    <br>
                    <ul>
                        <!-- agar ek review h tbhi section dikhao -->
                        <% if(listing.reviews.length>0) { %>
                        <h4 style="color:crimson">All Reviews</h4>
                        <div class="row">
                            
                            <!-- listings.reviews me ObjectId store h to populate k use krke review nikala -->
                            <% for(review of listing.reviews) { %>
                                <div class="card col-5 mb-3 ms-3 bg-body-tertiary">
                                    <!-- ms===margin-start -->
                                    <div class="card-body">
                                        <h5 class="card-title">@<%= review.author.username %></h5>
                                        <p class="starability-result card-text" data-rating="<%=review.rating%>">
                        
                                        </p>
                                        <p class="card-text"><%=review.comment%></p>
                                        <!-- to servser static result ofrating -->
                                        
                                        
                                        <form 
                                        method="POST" 
                                        action="/listings/<%=listing._id%>/reviews/<%=review._id%>?_method=DELETE">
                                            <button class="btn btn-dark btn-sm md-3">Delete</button>
                                        </form>
                                        <p class="card-text"><%=review.createdAt.toLocaleString().split(", ")[0]%></p>
                                    </div>
                                </div>

                                
                            <% } %>
                           

                        </div>
                        <% } %>

                        
                    </ul>
                    
                    
                </p>
                <hr>
            </div>
            <div class="col-8 mb-3">
                <h3>Where you'll be</h3>
                <p><%=listing.geometry.coordinates%></p>
                <!-- id=map hi dena hoga -->
                <div id="map"></div>
            </div>
            
        </div>

        

    </div>
    <!-- map ke liye -->
    <!-- taki js file me ye variable defined ho -->
    <script>
       
    </script>

    <!-- <script src="../js/map.js"></script> -->
    <script>
        // const coordinates=<%- JSON.stringify(listing.geometry.coordinates) %>;
        // const location=<%-JSON.stringify(listing.location)%>;
        const listing=<%- JSON.stringify(listing) %>;
        // TO MAKE THE MAP APPEAR YOU MUST
        // ADD YOUR ACCESS TOKEN FROM
        // https://account.mapbox.com
        mapboxgl.accessToken = 'pk.eyJ1IjoiZGVsdGEtc3R1ZHVlbnQiLCJhIjoiY2xvMDk0MTVhMTJ3ZDJrcGR5ZDFkaHl4ciJ9.Gj2VU1wvxc7rFVt5E4KLOQ';
        const map = new mapboxgl.Map({
            container: 'map',
            // Choose from Mapbox's core styles, or make your own style with Mapbox Studio
            style:'mapbox://styles/mapbox/satellite-streets-v12',
            // style: 'mapbox://styles/mapbox/streets-v12',
            // center: [12.550343, 55.665957],
            // center:listing.geometry.coordinates,
            center:listing.geometry.coordinates,
            
            
            zoom: 8
        });
    
//         // Create a default Marker and add it to the map.
//         const marker=new mapboxgl.Marker({color:"crimson"})
//         // const marker1 = new mapboxgl.Marker()

//         .setLngLat(coordinates)
//         // .setPopup(new mapboxgl.Popup({offset:25}))
//         // .setHTML("<h1>Hello world<h1>")
//         .addTo(map);

//         // const popup = new mapboxgl.Popup({offset: popupOffsets, className: 'my-class'})
            
//         const popup = new mapboxgl.Popup({offset: 25})
// // .setLngLat(e.lngLat)
// .setHTML("<h1>Hello World!</h1>")
// // .setMaxWidth("300px")
// .addTo(map);
//         // Create a default Marker, colored black, rotated 45 degrees.
//         // const marker2 = new mapboxgl.Marker({ color: 'black', rotation: 45 })
//         //     .setLngLat([12.65147, 55.608166])
//         //     .addTo(map);
            const marker=new mapboxgl.Marker({color:"crimson"})
            .setLngLat(listing.geometry.coordinates)
            .setPopup(
                new mapboxgl.Popup({offset:25})
                .setHTML(`<h3>${listing.location}<h3><p>Exact Location provided after booking<p>`)
            
            )
            .addTo(map);
    </script>
</body>
