<!-- yaha se sirf /chats/new          aur       /chats/id me req jayegi -->

<!-- layout keyword h joki ejs-mate me defined-->
<% layout("/layouts/boilerplate") %>
    <style>
        #filters{
            display:flex;
            flex-wrap:wrap;
            align-items: center;
        }
        .filter{
            text-align:center;
            margin-right:2rem;
            margin-top:1rem;
            opacity:0.7;
        }
        .filter:hover{
            opacity:1;
            cursor:pointer;
        }

        .tax-info{
            /* hidden me jagah leta dikhta nhi none me n jagah n dikhta */
            display: none;
        }
        
        .tax-toggle{
            border:1px solid black;
            border-radius:0.5rem;
            height:2.6rem;
            padding:1rem;
            /* margin-left:2rem; */
            background-color:crimson;
            display:flex;
            align-items: center;
            margin-right:2rem;
        }
        .tax-toggle:hover{
            background-color: rgb(183, 8, 43);
        }
        .top-divs{
            display: flex;
            justify-content: space-between;
        }
    </style>
<body>
    <br>

    
    <!-- %> -->
    <h2 style="color:crimson">All Listings</h2>
    
    <div class="top-divs">
        <form method="GET" action="/listings/new"> 
            <button class="btn btn-secondary">Create New List</button>
        </form>
        <div class="tax-toggle">
            <div class="form-check-reverse form-switch ">
                <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault">
                <label class="form-check-label" for="flexSwitchCheckDefault">Display Total after Taxes</label>
            </div>
              
        </div>

    </div>



    <br>


    <% if(allListings.length==0) { %>
        <h1 style="text-align: center;">OOPs, Currently Unavailable!</h1>
    <% } %>


    <!-- row bootstrap ki class -->
    <!-- chahte ek row me 1,2,3 cols hi aate to row-cols-md-x -->
    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3">
        <!-- ek (row) me 3 (cols) jb screen (lg)  -->
        <%   for(listing of allListings){  %>
            <!-- GET /listings/:id (show route) for specific list -->
            <!-- href me get req aygei MAY be bcz it is default req type-->
            <!-- bootstrap k card -->
            
            <a href="/listings/<%=listing._id%>" class="a-card-link">
                <div class="card
                listing-card" style="width:20rem">
                    <img style="height:20rem" src="<%=listing.image.url%>" class="card-img-top" alt="listing_image">
                    <!-- agar img ke upar koi text overlay krna -->
                    <!-- <div class="card-img-overlay">abcd</div> -->
                    <div class="card-body">
                        <p class="card-text">
                            <b><%=listing.title%></b>
                            <br>
                            &#8377; <%=listing.price.toLocaleString("en-IN") %> /night
                            <i class="tax-info">&nbsp;&nbsp;+18% GST</i>
                        </p>
                    </div>
                    <div class="card-img-overlay"><%=listing.title%></div>
                    
                </div>
            </a>
            

        <% } %>
    </div>

    <script>
        let taxSwitch=document.getElementById("flexSwitchCheckDefault");
        taxSwitch.addEventListener("click",()=>{
            let taxInfo=document.getElementsByClassName("tax-info");
            console.log(taxInfo);
            for(info of taxInfo)
            {
                if(info.style.display!="inline")
                info.style.display="inline";
                else
                info.style.display="none";
            }
        })
    </script>

</body>



